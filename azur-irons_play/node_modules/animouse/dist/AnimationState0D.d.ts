import { Emitter } from "eventail";
import { AnimationAction } from "three";
export declare class AnimationState0D extends Emitter {
    private action;
    private powerPrivate;
    constructor(action: AnimationAction);
    get power(): number;
    set power(value: number);
}
