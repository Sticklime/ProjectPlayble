import { AnimationMixer } from "three";
import { AnimationState } from "./AnimationState";
type Condition = (...args: unknown[]) => boolean;
interface ITransition {
    from?: AnimationState;
    to: AnimationState;
    duration: number;
    condition?: Condition;
}
export declare class AnimationStateMachine {
    private currentState;
    private fadingStates;
    private mixer;
    private transitions;
    private elapsedTime;
    constructor(initialState: AnimationState, mixer: AnimationMixer);
    addTransition(event: string, transition: ITransition): void;
    handleEvent(eventName: string, ...args: unknown[]): boolean;
    update(deltaTime: number): void;
    private transitionTo;
}
export {};
