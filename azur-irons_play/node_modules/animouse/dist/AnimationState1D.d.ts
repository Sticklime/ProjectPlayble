import { Emitter } from "eventail";
import { AnimationAction } from "three";
export type Action = {
    action: AnimationAction;
    value: number;
};
export declare class AnimationState1D extends Emitter {
    private actions;
    private powerPrivate;
    private blend;
    constructor(actions: Action[]);
    get power(): number;
    set power(value: number);
    setBlend(value: number): void;
    private updateAction;
    private update;
}
