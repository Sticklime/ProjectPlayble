import { Emitter } from "eventail";
import { AnimationAction } from "three";
export declare class AnimationState2D extends Emitter {
    private xPositive;
    private xNegative;
    private yPositive;
    private yNegative;
    private center;
    private powerPrivate;
    private blend;
    constructor(xPositive: AnimationAction, xNegative: AnimationAction, yPositive: AnimationAction, yNegative: AnimationAction, center: AnimationAction);
    get power(): number;
    set power(value: number);
    setBlend(x: number, y: number): void;
    private updateAction;
    private update;
}
