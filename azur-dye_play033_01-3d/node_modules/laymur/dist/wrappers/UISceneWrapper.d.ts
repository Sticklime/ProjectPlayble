import type { Object3D, WebGLRenderer } from "three";
import { Color, Matrix2, Matrix3, Matrix4, PlaneGeometry, Texture, Vector2, Vector3, Vector4 } from "three";
import { UIColor } from "../miscellaneous/UIColor";
import type { UISceneWrapperClientAPI } from "../miscellaneous/UISceneWrapperClientAPI";
import { UITransparencyMode } from "../miscellaneous/UITransparencyMode";
type UIPropertyType = UIColor | Color | Texture | Vector2 | Vector3 | Vector4 | Matrix2 | Matrix3 | Matrix4;
export declare const geometry: PlaneGeometry;
export declare class UISceneWrapper implements UISceneWrapperClientAPI {
    private readonly scene;
    private readonly camera;
    private readonly descriptors;
    private lastPlaneIndex;
    constructor(w: number, h: number);
    createPlane(source: string, uniforms: Record<string, UIPropertyType>): number;
    destroyPlane(handler: number): this;
    setTransform(handler: number, value: Matrix4): this;
    setUniform(handler: number, uniform: string, value: UIPropertyType): this;
    setTransparency(handler: number, mode: UITransparencyMode): this;
    setVisibility(handler: number, visible: boolean): this;
    insertCustomObject(object: Object3D): this;
    removeCustomObject(object: Object3D): this;
    resize(width: number, height: number): this;
    render(renderer: WebGLRenderer): this;
    private resolveDescriptor;
}
export {};
