import { InstancedBufferAttribute, Matrix2, Matrix3, Matrix4, Mesh, Texture, Vector2, Vector3, Vector4 } from "three";
import { UIColor } from "../miscellaneous/UIColor";
import { UITransparencyMode } from "./UITransparencyMode";
export type UIPropertyType = Texture | UIColor | Vector2 | Vector3 | Vector4 | Matrix2 | Matrix3 | Matrix4 | number;
export type UIPropertyTypeName = "Texture" | "UIColor" | "Vector2" | "Vector3" | "Vector4" | "Matrix2" | "Matrix3" | "Matrix4" | "number";
interface Descriptor {
    firstIndex: number;
    count: number;
}
export declare class UIGenericInstancedPlane extends Mesh {
    protected handlerToDescriptor: Map<number, Descriptor>;
    protected lastHandler: number;
    protected capacity: number;
    protected propertyBuffers: Map<string, InstancedBufferAttribute>;
    private readonly instancedGeometry;
    private readonly shaderMaterial;
    constructor(source: string, layout: Record<string, UIPropertyTypeName>, transparency?: UITransparencyMode, capacity?: number);
    createInstances(count: number): number;
    destroyInstances(handler: number): void;
    updateProperties(handler: number, offset: number, instancesProperties: Record<string, UIPropertyType>[]): void;
    updateTransforms(handler: number, offset: number, instancesTransforms: Matrix4[]): void;
    updateUVTransforms(handler: number, offset: number, uvTransforms: Matrix3[]): void;
    updateVisibility(handler: number, offset: number, instancesVisibility: boolean[]): void;
    destroy(): void;
    private resolveDescriptor;
    private resolveBufferAttribute;
    private resolveUniform;
    private resizeGeometry;
    private copyRangeData;
    private shiftRangesDown;
}
export {};
