import gsap from 'gsap';

const DEFAULT_X_FROM = 0;
const DEFAULT_X_TO = 0;
const DEFAULT_Y_FROM = 0;
const DEFAULT_Y_TO = 0;
const DEFAULT_SCALE_FROM = 0.5;
const DEFAULT_SCALE_TO = 1;
const DEFAULT_ALPHA_FROM = 0;
const DEFAULT_ALPHA_TO$1 = 1;
const DEFAULT_DELAY$2 = 0;
const DEFAULT_DURATION$2 = 0.25;
const DEFAULT_EASE$2 = "back.out(1.7)";
/**
 * Animator for element appearance effects.
 */
class UIAppearAnimator {
    /**
     * Animates elements appearing with position, scale, and alpha transitions.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     * @returns Promise that resolves when animation completes
     */
    static appear(target, options = {}) {
        const { xFrom = DEFAULT_X_FROM, xTo = DEFAULT_X_TO, yFrom = DEFAULT_Y_FROM, yTo = DEFAULT_Y_TO, scaleFrom = DEFAULT_SCALE_FROM, scaleTo = DEFAULT_SCALE_TO, alphaFrom = DEFAULT_ALPHA_FROM, alphaTo = DEFAULT_ALPHA_TO$1, delay = DEFAULT_DELAY$2, duration = DEFAULT_DURATION$2, ease = DEFAULT_EASE$2, } = options;
        const elements = Array.isArray(target) ? target : [target];
        const colorTarget = {
            duration,
            ease: "power1.inOut",
        };
        const microTarget = { duration, ease };
        if (xFrom !== xTo) {
            microTarget.x = xTo;
            for (const element of elements) {
                element.micro.x = xFrom;
            }
        }
        if (yFrom !== yTo) {
            microTarget.y = yTo;
            for (const element of elements) {
                element.micro.y = yFrom;
            }
        }
        if (scaleFrom !== scaleTo) {
            microTarget.scaleX = scaleTo;
            microTarget.scaleY = scaleTo;
            for (const element of elements) {
                element.micro.scaleX = scaleFrom;
                element.micro.scaleY = scaleFrom;
            }
        }
        if (alphaFrom !== alphaTo) {
            colorTarget.a = alphaTo;
            for (const element of elements) {
                element.color.a = alphaFrom;
            }
        }
        return new Promise((onComplete) => {
            const timeline = gsap.timeline({ delay, onComplete });
            for (const element of elements) {
                timeline
                    .to(element.micro, microTarget, 0)
                    .to(element.color, colorTarget, 0);
            }
        });
    }
}

const DEFAULT_X_OFFSET = 0;
const DEFAULT_Y_OFFSET = -25;
const DEFAULT_X_SCALE = 0.85;
const DEFAULT_Y_SCALE = 1;
const DEFAULT_DELAY$1 = 0;
const DEFAULT_DURATION_IN$2 = 0.125;
const DEFAULT_DURATION_OUT$2 = 0.25;
const DEFAULT_EASE_IN$2 = "power1.out";
const DEFAULT_EASE_OUT$2 = "back.out";
/**
 * Animator for click feedback effects.
 */
class UIClickAnimator {
    /**
     * Animates elements with click feedback including position offset and scale effects.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     * @returns Promise that resolves when animation completes
     */
    static click(target, options = {}) {
        const { xOffset = DEFAULT_X_OFFSET, yOffset = DEFAULT_Y_OFFSET, xScale = DEFAULT_X_SCALE, yScale = DEFAULT_Y_SCALE, delay = DEFAULT_DELAY$1, durationIn = DEFAULT_DURATION_IN$2, durationOut = DEFAULT_DURATION_OUT$2, easeIn = DEFAULT_EASE_IN$2, easeOut = DEFAULT_EASE_OUT$2, } = options;
        const elements = Array.isArray(target) ? target : [target];
        return new Promise((onComplete) => {
            const timeline = gsap.timeline({ onComplete });
            const inMicroTarget = {
                delay,
                duration: durationIn,
                ease: easeIn,
            };
            const outMicroTarget = {
                duration: durationOut,
                ease: easeOut,
            };
            if (xOffset !== 0) {
                inMicroTarget.x = xOffset;
                outMicroTarget.x = 0;
            }
            if (yOffset !== 0) {
                inMicroTarget.y = yOffset;
                outMicroTarget.y = 0;
            }
            if (xScale !== 1) {
                inMicroTarget.scaleX = xScale;
                outMicroTarget.scaleX = 1;
            }
            if (yScale !== 1) {
                inMicroTarget.scaleY = yScale;
                outMicroTarget.scaleY = 1;
            }
            for (const element of elements) {
                timeline
                    .to(element.micro, inMicroTarget, 0)
                    .to(element.micro, outMicroTarget);
            }
        });
    }
}

const DEFAULT_DELAY = 0;
const DEFAULT_DURATION$1 = 0.25;
const DEFAULT_EASE$1 = "back.in(1.7)";
const DEFAULT_ALPHA_TO = 0;
/**
 * Animator for element disappearance effects.
 */
class UIDisappearAnimator {
    /**
     * Animates elements disappearing with position, scale, and alpha transitions.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     * @returns Promise that resolves when animation completes
     */
    static disappear(target, options = {}) {
        const { delay = DEFAULT_DELAY, duration = DEFAULT_DURATION$1, ease = DEFAULT_EASE$1, alphaTo = DEFAULT_ALPHA_TO, } = options;
        const elements = Array.isArray(target) ? target : [target];
        const microTarget = { duration, ease };
        const colorTarget = {
            duration,
            ease: "power1.inOut",
            a: alphaTo,
        };
        if (options.xTo !== undefined) {
            microTarget.x = options.xTo;
        }
        if (options.yTo !== undefined) {
            microTarget.y = options.yTo;
        }
        if (options.scaleTo !== undefined) {
            microTarget.scaleX = options.scaleTo;
            microTarget.scaleY = options.scaleTo;
        }
        return new Promise((onComplete) => {
            const timeline = gsap.timeline({ delay, onComplete });
            for (const element of elements) {
                timeline
                    .to(element.micro, microTarget, 0)
                    .to(element.color, colorTarget, 0);
            }
        });
    }
}

const jumpCallTweens = new WeakMap();
const DEFAULT_JUMP_HEIGHT = 35;
const DEFAULT_SCALE_IN = 1.15;
const DEFAULT_SCALE_OUT = 1.15;
const DEFAULT_ITERATIONS$2 = -1;
const DEFAULT_COOLDOWN$2 = 3;
const DEFAULT_START_WITH_COOLDOWN$2 = true;
const DEFAULT_DURATION_IN$1 = 0.5;
const DEFAULT_DURATION_OUT$1 = 0.5;
const DEFAULT_EASE_IN$1 = "power1.out";
const DEFAULT_EASE_OUT$1 = "power1.in";
const DEFAULT_STOP_DURATION$2 = 0.25;
const DEFAULT_STOP_EASE$2 = "power1.inOut";
/**
 * Animator for jump call attention effects.
 */
class UIJumpCallAnimator {
    /**
     * Starts a jumping animation with scale and position effects.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     */
    static jump(target, options = {}) {
        const { jumpHeight = DEFAULT_JUMP_HEIGHT, scaleIn = DEFAULT_SCALE_IN, scaleOut = DEFAULT_SCALE_OUT, iterations = DEFAULT_ITERATIONS$2, cooldown = DEFAULT_COOLDOWN$2, startWithCooldown = DEFAULT_START_WITH_COOLDOWN$2, durationIn = DEFAULT_DURATION_IN$1, durationOut = DEFAULT_DURATION_OUT$1, easeIn = DEFAULT_EASE_IN$1, easeOut = DEFAULT_EASE_OUT$1, } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            const timeline = gsap.timeline({
                repeat: Number.isNaN(iterations) ? -1 : iterations,
                onComplete: () => {
                    jumpCallTweens.delete(element);
                },
            });
            if (startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            element.micro.anchorY = 0;
            timeline
                .to(element.micro, {
                scaleX: scaleIn,
                scaleY: 1 / scaleIn,
                duration: durationIn * 0.5,
                ease: "power1.inOut",
            })
                .to(element.micro, {
                scaleX: 1 / scaleIn,
                scaleY: scaleIn,
                duration: durationIn * 0.25,
                ease: "power1.inOut",
            })
                .to(element.micro, {
                scaleX: 1,
                scaleY: 1,
                duration: durationIn * 0.25,
                ease: "power1.inOut",
            })
                .to(element.micro, {
                y: jumpHeight,
                duration: durationIn * 0.375,
                ease: easeIn,
            }, durationIn * 0.625 +
                (startWithCooldown && cooldown > 0 ? cooldown : 0))
                .to(element.micro, {
                y: 0,
                duration: durationOut * 0.375,
                ease: easeOut,
            })
                .to(element.micro, {
                scaleX: scaleOut,
                scaleY: 1 / scaleOut,
                duration: durationOut * 0.225,
                ease: "power1.out",
            })
                .to(element.micro, {
                scaleX: 1,
                scaleY: 1,
                duration: durationOut * 0.4,
                ease: "power1.inOut",
            });
            if (!startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            jumpCallTweens.set(element, timeline);
        }
    }
    /**
     * Stops the jumping animation and returns elements to rest position.
     * @param target - Single element or array of elements to stop animating
     * @param options - Stop animation configuration options
     */
    static stopJump(target, options = {}) {
        const { duration = DEFAULT_STOP_DURATION$2, ease = DEFAULT_STOP_EASE$2 } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            const tween = jumpCallTweens.get(element);
            if (tween instanceof gsap.core.Timeline) {
                tween.kill();
                jumpCallTweens.delete(element);
                gsap.to(element.micro, {
                    y: 0,
                    duration,
                    ease,
                });
            }
        }
    }
}

const pulseCallTweens = new WeakMap();
const DEFAULT_SCALE = 1.1;
const DEFAULT_ITERATIONS$1 = -1;
const DEFAULT_COOLDOWN$1 = 3;
const DEFAULT_START_WITH_COOLDOWN$1 = true;
const DEFAULT_DURATION_IN = 0.25;
const DEFAULT_DURATION_OUT = 0.5;
const DEFAULT_EASE_IN = "power1.inOut";
const DEFAULT_EASE_OUT = "power1.inOut";
const DEFAULT_STOP_SCALE = 1;
const DEFAULT_STOP_DURATION$1 = 0.25;
const DEFAULT_STOP_EASE$1 = "power1.inOut";
/**
 * Animator for pulse attention effects.
 */
class UIPulseCallAnimator {
    /**
     * Starts a pulsing animation with scale effects.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     */
    static pulse(target, options = {}) {
        const { scale = DEFAULT_SCALE, iterations = DEFAULT_ITERATIONS$1, cooldown = DEFAULT_COOLDOWN$1, startWithCooldown = DEFAULT_START_WITH_COOLDOWN$1, durationIn = DEFAULT_DURATION_IN, durationOut = DEFAULT_DURATION_OUT, easeIn = DEFAULT_EASE_IN, easeOut = DEFAULT_EASE_OUT, } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            const timeline = gsap.timeline({
                repeat: Number.isNaN(iterations) ? -1 : iterations,
                onComplete: () => {
                    pulseCallTweens.delete(element);
                },
            });
            if (startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            timeline
                .to(element.micro, {
                scaleX: scale,
                scaleY: scale,
                duration: durationIn,
                ease: easeIn,
            })
                .to(element.micro, {
                scaleX: element.micro.scaleX,
                scaleY: element.micro.scaleY,
                duration: durationOut,
                ease: easeOut,
            });
            if (!startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            pulseCallTweens.set(element, timeline);
        }
    }
    /**
     * Stops the pulsing animation and returns elements to rest scale.
     * @param target - Single element or array of elements to stop animating
     * @param options - Stop animation configuration options
     */
    static stopPulse(target, options = {}) {
        const { scale = DEFAULT_STOP_SCALE, duration = DEFAULT_STOP_DURATION$1, ease = DEFAULT_STOP_EASE$1, } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            const tween = pulseCallTweens.get(element);
            if (tween instanceof gsap.core.Timeline) {
                tween.kill();
                pulseCallTweens.delete(element);
                gsap.to(element.micro, {
                    scaleX: scale,
                    scaleY: scale,
                    duration,
                    ease,
                });
            }
        }
    }
}

const spinCallTweens = new WeakMap();
const DEFAULT_ROTATION = 0.0435;
const DEFAULT_ANCHOR_X = 0.5;
const DEFAULT_ANCHOR_Y = 0.5;
const DEFAULT_SPIN_COUNT = 3;
const DEFAULT_ITERATIONS = -1;
const DEFAULT_COOLDOWN = 3;
const DEFAULT_START_WITH_COOLDOWN = true;
const DEFAULT_DURATION = 1;
const DEFAULT_EASE = "power1.inOut";
const DEFAULT_STOP_DURATION = 0.25;
const DEFAULT_STOP_EASE = "power1.inOut";
/**
 * Animator for spin call attention effects.
 */
class UISpinCallAnimator {
    /**
     * Starts a spinning animation with rotation oscillations.
     * @param target - Single element or array of elements to animate
     * @param options - Animation configuration options
     */
    static spin(target, options = {}) {
        const { rotation = DEFAULT_ROTATION, anchorX = DEFAULT_ANCHOR_X, anchorY = DEFAULT_ANCHOR_Y, spinCount = DEFAULT_SPIN_COUNT, iterations = DEFAULT_ITERATIONS, cooldown = DEFAULT_COOLDOWN, startWithCooldown = DEFAULT_START_WITH_COOLDOWN, duration = DEFAULT_DURATION, ease = DEFAULT_EASE, } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            element.micro.anchorX = anchorX;
            element.micro.anchorY = anchorY;
            const timeline = gsap.timeline({
                repeat: Number.isNaN(iterations) ? -1 : iterations,
                onComplete: () => {
                    spinCallTweens.delete(element);
                },
            });
            if (startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            const spinDuration = duration / (spinCount + 2);
            timeline.to(element.micro, {
                rotation,
                duration: spinDuration,
                ease,
            });
            let currentRotation = rotation;
            for (let i = 0; i < spinCount; i++) {
                timeline.to(element.micro, {
                    rotation: (currentRotation *= -1),
                    duration: spinDuration,
                    ease,
                });
            }
            timeline.to(element.micro, {
                rotation: 0,
                duration: spinDuration,
                ease,
            });
            if (!startWithCooldown && cooldown > 0) {
                timeline.to({}, { duration: cooldown });
            }
            spinCallTweens.set(element, timeline);
        }
    }
    /**
     * Stops the spinning animation and returns elements to rest rotation.
     * @param target - Single element or array of elements to stop animating
     * @param options - Stop animation configuration options
     */
    static stopSpin(target, options = {}) {
        const { duration = DEFAULT_STOP_DURATION, ease = DEFAULT_STOP_EASE } = options;
        const elements = Array.isArray(target) ? target : [target];
        for (const element of elements) {
            const tween = spinCallTweens.get(element);
            if (tween instanceof gsap.core.Timeline) {
                tween.kill();
                spinCallTweens.delete(element);
                gsap.to(element.micro, {
                    rotation: 0,
                    duration,
                    ease,
                });
            }
        }
    }
}

export { UIAppearAnimator, UIClickAnimator, UIDisappearAnimator, UIJumpCallAnimator, UIPulseCallAnimator, UISpinCallAnimator };
//# sourceMappingURL=index.js.map
